
import { Project } from './types';

// Los datos ahora se cargan dinámicamente desde el servicio de datos
// Se mantiene una estructura vacía o mínima para compatibilidad si fuera necesario
export const PROJECTS: Project[] = [];
